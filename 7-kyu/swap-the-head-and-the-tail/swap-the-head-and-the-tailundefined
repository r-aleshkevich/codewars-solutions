def swap_head_tail(arr):
    if len(arr) < 2:
        return arr[:]
    mid = len(arr) // 2
    head = arr[:mid]
    tail = arr[-mid:]
    middle = arr[mid:-mid]
    return tail + middle + head
    
    